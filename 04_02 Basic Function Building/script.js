function findBiggestFraction() {
    //console.log("The function is running!");
    a>b ? console.log("a: ", a) : console.log("b: ", b)
}

// must define variables before calling them so JavaScript doesn't automatically make them and break the code
// order that things are placed in JavaScript is essential
var a = 3/4;
var b = 5/7;



findBiggestFraction();
